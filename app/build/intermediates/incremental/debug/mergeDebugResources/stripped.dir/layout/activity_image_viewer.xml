<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- ImageView para a imagem em fullscreen -->
    <ImageView
        android:id="@+id/fullscreenImageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"
        android:contentDescription="Imagem em tela cheia" />

    <!-- View personalizada para desenho livre -->
    <com.example.floatingbutton.FreeDrawCropView
        android:id="@+id/freeDrawCropView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Botões flutuantes sobre a imagem -->
    
    <!-- Botão de fechar (X) no canto superior direito -->
    <ImageButton
        android:id="@+id/btnClose"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="16dp"
        android:background="@drawable/circular_button_background"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:tint="@android:color/white"
        android:contentDescription="Fechar" />

    <!-- Botão de desenho livre no canto superior esquerdo -->
    <ImageButton
        android:id="@+id/btnCrop"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_margin="16dp"
        android:background="@drawable/circular_button_background"
        android:src="@android:drawable/ic_menu_edit"
        android:tint="@android:color/white"
        android:contentDescription="Desenho Livre" />

    <!-- Botão de limpar desenho no centro esquerdo -->
    <ImageButton
        android:id="@+id/btnClear"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_centerVertical="true"
        android:layout_alignParentStart="true"
        android:layout_margin="16dp"
        android:background="@drawable/circular_button_background"
        android:src="@android:drawable/ic_menu_revert"
        android:tint="@android:color/white"
        android:contentDescription="Limpar Desenho" />

    <!-- Botão de salvar no canto inferior direito -->
    <ImageButton
        android:id="@+id/btnSave"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="16dp"
        android:background="@drawable/circular_button_background"
        android:src="@android:drawable/ic_menu_save"
        android:tint="@android:color/white"
        android:contentDescription="Salvar" />

    <!-- Botão de compartilhar no canto inferior esquerdo -->
    <ImageButton
        android:id="@+id/btnShare"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_margin="16dp"
        android:background="@drawable/circular_button_background"
        android:src="@android:drawable/ic_menu_share"
        android:tint="@android:color/white"
        android:contentDescription="Compartilhar" />

</RelativeLayout>
